<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:devsko.LayoutAnalyzer">

    <Style x:Key="PropertyStyle" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static local:LayoutControl.ForegroundKey}}"/>
        <Setter Property="Padding" Value="3,1,3,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static local:LayoutControl.TreeViewItemInactiveSelectedBackgroundKey}}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Grid.Column" Value="0" />
    </Style>
    <Style x:Key="PropertyValueStyle" TargetType="{x:Type Label}" BasedOn="{StaticResource PropertyStyle}">
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Grid.Column" Value="1" />
    </Style>

</ResourceDictionary>
